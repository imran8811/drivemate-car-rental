import { RemoveDecimalsPipe } from './remove-decimals.pipe';

describe('RemoveDecimalsPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveDecimalsPipe();
    expect(pipe).toBeTruthy();
  });
});
